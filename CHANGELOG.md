# 更新日志

本文件记录 Kiro Manager Lite 的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.21] - 2026-09-09

本版本重写内置浏览器：补上后退 / 前进 / 刷新工具条、地址栏与更多菜单，
并去掉拖慢每个请求的 webRequest 监听。同时修复主页可用额度可能为负数的算法问题，
新增控件尺寸设置与分割导出。

### 新增

#### 内置浏览器工具条

- 「前往官网」窗口改为 `BaseWindow` + 两个 `WebContentsView`（工具条 + 站点内容），
  顶部提供后退 / 前进 / 刷新、居中标题、右上角「更多」菜单与底部加载进度条；
  macOS 下隐藏原生标题栏，红绿灯压在工具条上
- 第二行新增地址栏：可输入任意网址回车跳转，非网址按百度搜索。
  地址随导航自动更新，正在输入时不会被覆盖，Esc 还原
- 「更多」菜单提供刷新、强制重新加载、IP 查询（三个站点交叉比对）、打开百度、
  复制链接、在默认浏览器打开、开发者工具。IP 查询与打开百度都开新窗口，不顶掉当前页面
- 从内置浏览器弹出的窗口同样带完整工具条与地址栏，嵌套弹窗递归生效
- 补齐 `⌘/Ctrl + [ ` / `] ` / `R` / `W` 导航快捷键

#### 加载流程

- 窗口先隐藏，等工具条就绪再显示，避免「刚打开就整块白屏」
- 首屏看门狗：`dom-ready` 前卡住超过 6 秒即中止重来，最多 4 次。
  实测首屏挂住后重发往往 1 秒内成功，起作用的是重试而非绕缓存
- 重试进度实时显示在标题位（「连接不上，正在重试（2/4）…」），次数用尽才显示错误页；
  停在错误页时点刷新会重新访问真实地址，而不是重刷错误页本身
- 弹窗只对首次导航套看门狗，之后的表单提交与登录回调一律不重发，避免丢掉 POST 内容

#### 控件尺寸设置

- 「外观」新增控件尺寸单选：默认尺寸 / 大尺寸。此前全局固定为大尺寸，
  现可切到默认尺寸以在一屏放下更多内容；默认值保持大尺寸，升级后观感不变

#### 分割导出

- 选中多个账号时，导出按钮右侧新增下拉：「导出为压缩包」与「逐个分割导出」
- 分割导出为每个账号生成一个独立文件，外加一个整批的 `all` 文件，一起打成压缩包。
  全部文件都用当前选中的格式（选 JSON 得到 N 个 `.json` + `all.json`，TXT 则为 `.txt` + `all.txt`）
- 包内单账号文件沿用单个账号导出时的命名，取出后可直接回导；
  昵称重复会自动补 `-2`、`-3`，压缩包名与多账号导出同一套格式
- 压缩复用 xlsx 已有的 ZIP 实现，未引入额外依赖

### 修复

#### 主页可用额度可能为负数

- 「剩余额度」改名为「可用额度」，并改为逐账号 clamp 后求和（`Σ max(0, 额度 − 已用)`），
  不再用「总额度 − 总已用」。后者在账号降档时会算出负数：原本 2000 额度用掉 1900、
  之后掉成 50 积分的普通号，这一个账号就贡献 −1850，把汇总值拖成负数，
  而它真实的可用额度是 0
- 使用率改按 `Σ min(已用, 额度)` 计算，天然落在 0~100，不再出现 3800% 这类数值
- 原先的红色「已超额」告警是误判——已用超过当前额度并非超额消费，而是额度变小了。
  改为黄色提示，说明是订阅降档所致、这些账号的可用额度按 0 计算

#### 导入后排序散落

- 导入账号的 `createdAt` 一律按导入时刻重排，不再沿用文件里的原值。
  列表默认按添加时间倒序，沿用原值会让刚导入的账号按历史时间散落在列表各处
- 批内按输入下标定序，列表顺序与文件顺序一致；此前精简 JSON 导入是并发校验，
  批内排列取决于完成先后，看着像乱序
- 时间基准取「现在」与库中最大 `createdAt` 的较大值，避免大批量连续导入时两批区间交叠

### 优化

- 移除内置浏览器会话上的 `webRequest` 监听。注册该监听后 Chromium 每个请求都要经主进程
  IPC 往返并等待回调放行，一个页面上百个子资源就是上百次阻塞式往返，
  这正是「同一台机器 Chrome 不卡、内置浏览器卡」的根源。UA 与语言改用
  `session.setUserAgent` 配合 `app.userAgentFallback` 达成，后者是弹窗首个请求唯一生效的途径
- 内置浏览器视图关闭后台降频、拼写检查，并开启字节码缓存
- 积分变化弹窗高度上限提高，一屏可显示更多明细行

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.20] - 2026-09-05

本版本修复社交登录账号批量导入时被误判为 IdC、整批报「需要同时提供 Client ID 与 Client Secret」
的问题，并为导入日志补上一键复制。

### 修复

#### 社交登录账号批量导入整批失败

- 部分导出文件用 snake_case 命名，登录方式写在 `login_provider` 而非 `provider` / `idp`。
  此前解析不到该字段就默认按 BuilderId 处理，进而以 IdC 口径校验并要求
  clientId / clientSecret——社交账号本就没有这两个值，于是整批「校验失败」
- 精简 JSON 的字段解析改为按别名取值，一次覆盖已知写法：
  登录方式认 `provider` / `idp` / `login_provider` / `loginProvider` / `login_option`，
  refreshToken、clientId、clientSecret、region、email 也同时兼容 camelCase 与 snake_case
- 这类导出常把整份 `kiro-auth-token.json` 原样嵌在 `kiro_auth_token_raw` 里，
  现会先展开该对象再用外层字段覆盖；登录方式仍缺失时按其中的 `authMethod` 兜底
- 兜底判定：即便登录方式仍无法识别，只要没有 clientId / clientSecret 就按社交登录处理。
  IdC 刷新必须带这两个值，缺失时不可能是 IdC——这样遇到未见过的导出格式也不会整批失败

### 新增

- 导入日志标题右侧新增「复制全部」，一键复制完整日志。界面为控制 DOM 数量只渲染前 200 条，
  但复制取的是全量内容并提示真实条数，便于排查被截断的那部分
- 从文件导入与粘贴文本导入两个弹窗同步支持

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.19] - 2026-08-25

本版本新增账号 API Key 删除与批量设置备注，修复 Builder ID 生成 / 列表 API Key 报错、
Enterprise 账号打开官网停在登录页的问题，并优化了几处弹窗体验。

### 新增

#### 账号 API Key 删除

- 「API Key 管理」列表每行新增删除按钮，二次确认后调用控制面 `DeleteApiKey`，
  删除成功再重新拉取列表刷新界面；删除中仅该行显示加载态，不影响其它行

#### 批量设置备注

- 账户管理页选中一个或多个账号后，删除按钮左侧出现「批量操作」下拉，
  内含「批量设置备注」：弹窗输入后覆盖所选账号的备注，留空则清空
- 作用范围与删除、导出一致（全部勾选项，不受当前搜索影响）；
  一次性写入而非逐个保存，选中上百个也只落盘一次

### 修复

#### Builder ID 生成 / 列表 API Key 报错 profileArn 必填

- 控制面（`CreateApiKey` / `ListApiKeys` / `DeleteApiKey`）已把 profileArn 列为必填，
  不带就回 400 `Member must not be null`。此前候选构造会把 Builder ID 占位符剥掉，
  导致 Builder ID 账号生成与查看列表都失败；现改为按登录方式补齐占位符 / social ARN，
  与用量、模型列表接口口径一致

#### Enterprise 账号打开官网停在登录页

- Enterprise（IdC / SSO）账号打开 Kiro 官网后台会停在登录页：门户对这类账号
  还要求带 `ProfileArn` cookie，缺了就把会话判为 stale。现补齐该 cookie，
  实测 user-status 从 stale 变 active；对社交 / Builder ID 账号带上它无副作用
- 账号从未存过 profileArn 时（多为旧数据），打开前会现查一次 `ListAvailableProfiles`
  补齐，仅对 Enterprise 触发，失败静默降级不影响打开流程

### 优化

- 生成 API Key 的名称改为非必填：留空时按时间戳生成一个 8 位随机名提交，
  输入框补充占位说明
- 「API Key 管理」列表区分三态：首次加载只显示加载态，加载失败显示错误结果页
  （不再露出「0 个 / 暂无数据」这类会被误读为「没有 Key」的信息），成功后才显示表格
- 积分变化弹窗高度上限提高，一屏可显示更多明细行
- 批量设置备注弹窗与「编辑账号」的备注输入保持一致，不限制字数

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.18] - 2026-08-25

本版本修复 Enterprise（IAM Identity Center SSO）账号在线登录授权成功后，
校验环节报 `403 Invalid token` 导致账号加不进来的问题。

### 修复

#### Enterprise 账号补错 profileArn 被判 Invalid token

- 1.0.17 为满足「profileArn 变必填」给校验环节补了默认 ARN，但对 Enterprise 补出来的是
  内置兜底值，而该 ARN 属于另一个组织——拿别家 profile 查自己的用量，上游回
  `403 Invalid token`（与 Builder ID 那个 `User is not authorized to make this call.`
  是两条不同的错误，前者专指 profileArn 不对）
- Enterprise 的正确 ARN 只能向 `ListAvailableProfiles` 查。切号一直是这么做的，
  校验环节此前从未查过，于是补默认值时必然补错

#### profileArn 改为候选逐个实测

- 用量查询不再「猜一个 ARN 送出去」，改为按成功率依次尝试：
  账号已存的 ARN → Enterprise 的真实 profile → 按登录方式的默认值 → 不带
- 只在 ARN 被拒时（授权维度错误，或上游明确点名 profileArn）才换下一个；
  封禁与网络类错误立即抛出，不浪费请求
- 校验与状态刷新共用这一套，两条链路的 ARN 口径不会再各走各的

### 优化

- 实测生效的 profileArn 会随快照回传并持久化到账号，后续刷新一次命中；
  Enterprise 因此不必每轮都重新查一遍 `ListAvailableProfiles`
- `VerifyCredentialsInput` 补上 `profileArn` 声明：在线登录本就会带回上游给出的真实 ARN，
  此前类型里没有该字段，等于拿到了却用不上、还要再猜一遍。Enterprise 在线登录现在
  第一顺位就用上游返回的那个

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.17] - 2026-08-25

本版本修复 Builder ID 账号刷新用量、测活拉模型与对话全部返回
`403 User is not authorized to make this call.` 的问题。根因有两个，都在服务端改了口径：
请求头里的客户端版本号被用作准入条件，而 `profileArn` 从可选变成必填。

### 修复

#### 客户端版本号被服务端用作准入条件

- 请求 UA 从 `KiroIDE-0.6.18` / `aws-sdk-js-1.0.18` 升到 `KiroIDE-0.12.155` /
  `aws-sdk-js-1.0.34`。实测同一个 Builder ID token，仅替换版本号即可让
  `ListAvailableModels` 与 `generateAssistantResponse` 从 403 变 200
- 该门槛只作用于 Builder ID / IdC：Github / Google 两组 UA 都通，所以此前一直没暴露
- 排除法确认版本号是唯一变量：`x-amzn-codewhisperer-optout`、`x-amzn-kiro-agent-mode`、
  `origin` 取值、`accept` 头、q / codewhisperer 两个 host、GET / POST 都不影响结果
- UA 里的 os / node 指纹改为按本机真实值生成，不再固定写 `os/windows` 与 `nodejs#20.16.0`

#### profileArn 由可选变为必填

- 用量、模型列表、对话三条链路统一补齐 `profileArn`，Builder ID 用 Kiro IDE 那个
  硬编码占位符 ARN。不带会被拒：用量回 403，模型列表回 400 `Invalid profileArn`，
  对话回 400 `profileArn is required for this request.`
- 新增 `usageProfileArn`：账号自己存了 ARN 就用它，没存则按登录方式补
  （社交 → 固定 social ARN，Enterprise → 真实 profile，其余 → Builder ID 占位符）
- 修复三处此前不带或会剥掉占位符的调用点：刷新用量、添加账号验活、切号前实测 ARN。
  其中验活那处原先固定传 `undefined`，会让新增的 Builder ID 账号直接验活失败
- 模型列表与对话的候选 ARN 补上 Builder ID 占位符：该登录方式没有 profile 概念，
  `ListAvailableProfiles` 对它明确返回「不支持」，此前候选里只剩「不带」，必然被拒

### 优化

- 批量刷新的并发通道启动时错峰，不再同一帧集中打出请求
- 重试退避加入随机抖动；重试与否改为采信发起端按状态码给出的显式结论，
  不再从错误文案反推——同一句 403 在刷新端点与用量端点上含义相反，靠文案分类必然出错

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.16] - 2026-08-23

本版本把备注纳入隐私打码范围，并让导出完成后自动在文件管理器里定位到刚导出的文件。

### 新增

#### 导出后定位文件

- 导出成功后自动打开所在文件夹并选中该文件，省掉自己翻目录这一步；
  账号导出、API Key 导出、API Key 详情单独导出、日志导出、表格导出五个入口全部覆盖
- 「数据管理」新增「导出后」选项，可切换为「不打开文件夹」，
  连续批量导出时不再被反复弹出的窗口打断；该选项持久化保存，默认打开文件夹
- 用 `showItemInFolder` 而非 `openPath`：后者会直接用默认程序打开文件内容，
  导出的多是凭证类文件，被自动弹开并非预期行为
- 定位失败不影响导出结果——文件此时已写入成功，没有理由因为唤不起文件管理器就报导出失败

### 优化

#### 备注纳入隐私打码

- 开启隐私打码后，备注整段显示为 `****`。备注是自由文本，用户常往里写人名、
  用途、购买渠道这类比邮箱更敏感的内容，此前却是全局唯一不打码的字段
- 邮箱靠 md5 前缀保证同一账号的遮罩串稳定、便于横向比对，备注没有这种结构，
  因此不做保留首尾的部分遮罩，直接整段替换；空备注仍显示 `-`，
  「有备注但被遮住」与「没写备注」在界面上依然可区分
- 覆盖全部会显示备注的位置：账号卡片、API Key 卡片、两个详情抽屉（含 API Key
  抽屉标题）、网关调用历史弹窗标题、批量测活的行内备注、页头当前 Key 标签，
  以及删除与切换 Key 的确认文案。只遮卡片而详情仍是明文等于没遮
- 悬停提示同步处理，隐私模式下不再挂原文；编辑弹窗内的备注输入框保持明文，
  搜索与排序仍按原始值执行

#### 其它

- 账号卡片的备注在无内容时显示 `备注：-` 占位，同排卡片高度恒定，
  虚拟滚动的行高不再因个别卡片多一行而跳动

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.15] - 2026-08-21

本版本补齐「前往官网」的内置浏览器：导航不再漏到系统浏览器、窗口尺寸放大，
并新增可自定义的浏览器地区设置；同时统一了账号与 API Key 的导出范围规则，
账号卡片补上备注展示。

### 新增

#### 内置浏览器地区

- 系统设置新增独立的「内置浏览器」卡片，可指定应用内打开的网页使用哪个地区，
  内置 50 个常用地区（标签按「地区（BCP 47）」展示，下拉支持搜索地区名与标签），
  并可选「自定义…」直接填写任意 BCP 47 标签
- 该设置同时作用于 `Accept-Language` 请求头与 Chromium 的界面区域：
  前者保存即生效，后者要在 app ready 前通过 `--lang` 指定，
  因此选择器旁标注了「修改地区后需要重启应用才能完全生效」
- 只影响应用内打开的网页，不改变本应用界面语言，也不影响账号所属的 AWS 区域

#### API Key 导出弹窗

- API Key 导出改用与账号导出同款弹窗，替换原先的纯文字确认框，提供两种格式：
  「APIKey-地区」（每行 `apikey----地区`，分隔符与卡密格式一致，可回导并还原区域）
  与「APIKey」（每行一个完整 Key，便于直接粘贴使用）
- 支持导出到文件或直接复制到剪贴板，文件名统一带秒级时间戳

### 优化

#### 前往官网

- 导航全程留在应用内：此前只放行 kiro.dev，点到支付页、AWS 文档这类站外链接
  就跳到系统浏览器，而那边没有这份会话，流程直接断掉。现在 http(s) 一律用
  应用内新窗口承载，并对新窗口递归挂上同样规则，第二层弹窗也不会外泄；
  mailto 等非 http(s) 协议 Electron 渲染不了，仍交给系统处理
- 窗口尺寸由 1180×900 提升到 1600×1200，并按当前显示器可用区域收边，
  1080p 等较矮的屏幕不会被系统裁掉或顶出屏幕

#### 导出范围

- 账号与 API Key 的导出统一为一条规则：有勾选就导出勾选的，没勾选就导出全部，
  不再让用户在弹窗里二次选择范围。原先账号导出弹窗会同时出现
  「已选 N 个 / 全部 M 个」切换，与列表里已经做过的勾选重复表达，容易误读
- 弹窗标题只保留一个标签说明本次会导出什么。账号详情抽屉的导出入口
  因此不再需要额外的范围锁定开关
- API Key 在未勾选时的导出范围由「当前筛选结果」改为全部，与账号侧口径一致

#### 其它

- 账号卡片在昵称下方显示备注，与 API Key 卡片一致；无备注时显示 `备注：-` 占位，
  保证同排卡片高度恒定，虚拟滚动的行高不会因个别卡片多一行而跳动

### 维护

- 清理无引用代码：删除 4 个无调用方的函数（含网关统计里一条永远为空的变更回调
  及其调用点），9 个仅在本文件内使用的导出收窄为模块私有
- 移除未被任何代码引用的生产依赖 `@electron-toolkit/preload`，
  不再随安装包一起分发

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.14] - 2026-08-17

本版本新增「前往官网」：用账号自己的凭证在应用内的私密窗口直接登录 Kiro 官网后台，
不必先切号再手动登录。

### 新增

#### 前往官网

- 账号卡片底部（编辑左侧）与账号详情抽屉（导出左侧）各新增入口，
  点击即用该账号的凭证打开 Kiro 官网后台
- 使用应用内的一次性会话：分区不带持久化前缀，只存在内存中，应用退出即清，
  既不与主窗口共用登录态，也不会碰用户自己浏览器里的身份
- 每次打开前先清空该会话的 cookie。残留的上一个账号 cookie 会让门户继续按旧身份渲染，
  表现就是「点了 A 账号却进了 B 账号的后台」
- 窗口按第三方页面对待：开启上下文隔离与沙箱、关闭 Node 集成；
  站外链接交给系统浏览器打开，只在窗口内放行 kiro.dev
- 请求头伪装成普通 Chrome：默认 UA 会带上应用名与 `Electron/<版本>`，
  是显眼的自动化特征。同时改写 `sec-ch-ua` 等客户端提示并清掉残留标识，
  只改 UA 字符串并不够。版本号取内置 Chromium 的真实主版本，
  声称版本与实际引擎能力对不上本身也是破绽

### 优化

- 账号详情抽屉加宽到 720px，窄窗口下按视口 92% 自适应，
  与 API Key 详情抽屉同一口径，长凭证不再挤成多行

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.13] - 2026-08-16

本版本新增账号级的 API Key 管理：直接用账号凭证向 Kiro 控制面申请 Key、查看该账号已创建的
Key 列表，同时把账号卡片与工具栏上并列的两个刷新入口收进下拉菜单。

### 新增

#### 账号 API Key 管理

- 账号卡片新增「API Key 管理」入口，用该账号凭证调用 Kiro 控制面的
  `CreateApiKey` 生成新 Key，并通过 `ListApiKeys` 展示它已创建的全部 Key
- 弹窗内表格展示名称、Key 前缀与创建时间，可随时刷新；标题带上所属账号邮箱，
  按视口高度固定为 80%，列表为空时同样保留表格区域高度，弹窗尺寸不随行数跳动
- 生成成功后单独弹窗展示完整明文并提供一键复制：上游只在创建时返回一次 `rawKey`，
  因此弹窗关闭即丢弃，之后列表里只剩前缀；未复制就关闭会二次确认
- 创建时间按上游的秒级浮点时间戳换算为毫秒，统一显示为 `YYYY-MM-DD HH:mm:ss`
- profileArn 按「账号自带 → 社交账号固定 ARN → Enterprise 真实 profile」依次尝试，
  只有授权维度的错误才换候选重试；accessToken 过期时自动刷新一次并把轮换后的凭证同步回本地

### 优化

- 账号卡片底部并列的「刷新密钥」「刷新用量与积分」合并为一个下拉菜单，
  腾出的钥匙图标改用于「API Key 管理」；菜单按钮的加载态取自任一子动作，
  操作反馈与此前的单个图标一致
- 账户管理工具栏的两个刷新按钮同样合并为「刷新密钥/用量/积分」下拉菜单，
  触发按钮上直接显示正在跑的任务进度，不必展开菜单才能看到状态
- 新增固定格式的日期时间工具，不再依赖 `toLocaleString` 的本地化输出
  （中文环境下月日不补零，列表里对不齐）

### 修复

- 修复从自定义层级的弹窗里发起二次确认时，确认框被弹窗盖住导致按钮点不到的问题：
  `Modal.confirm` 固定使用默认层级，现在支持显式传入更高的 `zIndex`
- 生成结果新增前缀校验：`keyPrefix` 同样以 `ksk_` 开头，一旦被当成完整 Key，
  用户会复制到一个看起来正常却不可用的短串，现在这种情况直接报错

### 文档

- README 补充账号 API Key 管理的功能说明与截图

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.12] - 2026-08-14

本版本重点优化大量账号与 API Key 场景下的页面切换和列表渲染性能，重做手动检查更新的
加载与失败反馈，并完善历史曲线显示和删除后的关联数据清理。

### 优化

#### 大数据列表与页面切换

- 移除页面路由切换时先退出再进入的过渡流程，避免账号或 API Key 较多时出现明显白屏，
  页面切换响应更直接
- API Key 卡片列表改用虚拟网格，只渲染当前视口附近的卡片；上百个 Key 时不再一次性创建
  全部卡片 DOM，滚动和进入页面更流畅
- 账号详情、API Key 详情、积分变化、网关调用历史、测活、导入、导出等重型弹窗与抽屉
  改为用户点击后再实时挂载，关闭后卸载，减少列表首屏的组件实例、监听器和图表开销
- 账号与 API Key 的删除改为局部更新状态，降低大列表操作时不必要的整表持久化与重渲染

#### 检查更新

- 关于页手动点击「检查更新」后立即打开结果弹窗并显示加载态，不再等待网络请求结束后
  才给出反馈，重复点击会复用同一次请求
- GitHub API 请求增加超时重试；接口不可用或频率受限时自动切换到 Releases 最新版本页面，
  从最终跳转地址识别版本号，降低偶发 `fetch failed` 对更新检查的影响
- 请求失败时弹窗显示明确的错误图标、标题和可读的网络提示，并提供「重新检查」与
  「手动更新」入口；手动更新会打开项目 GitHub 主页
- 冷启动、多窗口与手动检查同时触发时合并为一组远端请求，避免并发请求放大失败概率

#### 历史图表

- 账号积分、API Key 积分与网关调用历史的折线改为单调三次平滑曲线，转折更自然且不会
  在相邻数据点范围外产生过冲
- 调整 X 轴标签布局，最右侧时间完整显示；收紧网关调用历史图表左边距，扩大有效绘图区

### 修复

#### 删除数据一致性

- 删除账号时改为基于主进程最新快照执行，避免渲染进程旧数据覆盖主动续期刚写入的新凭证；
  同时从滚动备份中移除已删除账号，防止历史备份残留凭证和用量快照
- 删除 API Key 时同步清理积分变化历史与网关调用统计，并用进程级墓碑拦截删除前已发出的
  流式响应或延迟重试，防止已删除 Key 的孤儿统计被重新写回
- 批量删除后按当前剩余 Key 集合清理历史数据，避免长期积累无法再访问的残留记录

### 文档

- README 同步到 v1.0.12，按当前实现补充 API Key 管理、本地网关、真实调用统计、
  错误自动续接、常用工具与大数据列表优化等功能介绍，并更新最新维护日期

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.11] - 2026-08-13

本版本给 API Key 网关补上真实调用统计与历史曲线，新增遇到限流自动重试的能力，
并修掉社交登录回调被 Kiro IDE 抢走这个存在已久的问题。

### 新增

#### 网关调用统计

- API Key 卡片在用量下方新增四个统计模块：请求数、成功率、RPM、积分消耗，
  数据全部来自网关对真实请求的观测，没有调用记录时以 `-` 占位保持卡片高度一致
- 积分消耗取自响应流里的 `MeteringEvent`，是 Kiro 的计费口径与服务端权威值，
  不是本地估算
- 点击任意统计模块打开「网关调用历史」弹窗：可在请求数 / 成功率 / 积分消耗
  三条曲线间切换，配合汇总数据与按分钟聚合的明细表，支持清空
- 统计与历史持久化到独立存储文件，重启应用或重开网关都不会归零；
  按分钟桶聚合，每个 Key 最多保留 5000 条记录
- 对话请求与辅助请求（`/mcp`、模型列表等）分开计数：`/mcp` 在 API Key 鉴权下
  会稳定失败，混在一起会让「对话其实全部成功」显示成 40% 这类误导数字

#### 网关错误自动续接

- 常用工具页新增「网关错误自动续接」开关（默认关闭）：网关在把响应交给 IDE 之前
  拦下可恢复的错误并自动重发，对话不再中断、不需要手动点继续
- 始终使用同一个 API Key 重发，不会替换成别的 Key，额度消耗仍记在当前 Key 上
- 可勾选触发重试的状态码，共 9 项（429 / 500 / 502 / 503 / 504 / 408 / 402 / 403 / 409），
  每项带中文说明与推荐标记，默认只勾选纯粹的临时性故障
- 重试次数与间隔均可自定义，默认 10 次 / 100ms。采用固定间隔而非指数退避：
  Kiro 的速率限流恢复很快，退避到几秒只是白等，固定短间隔重试更多次反而更快
- 本周期额度已用尽（按小时 / 日 / 周 / 月计数）的情况一律不重试，直接透传，
  避免把额度问题无声掩盖成「有点慢」
- 辅助接口的失败不参与重试：它们不影响对话，重试只会让每次对话白等十几秒

### 优化

- 系统日志向上翻看历史时不再被新日志强行拽回底部；离开底部后右下角出现
  「回到最新」悬浮按钮，点击平滑滚动到底并恢复跟随
- 网关失败日志补上请求路径与上游返回的真实原因（`message` / `reason`），
  不再只有一个状态码全靠猜
- 默认窗口尺寸调整为 1600×1200，并按当前显示器可用区域自适应收窄，
  避免在较矮的屏幕上一开窗就超出工作区
- API Key 详情抽屉加宽并新增 User ID 展示（取自 `Get-Usage-Limits` 的 `userInfo`），
  邮箱与 User ID 均支持一键复制完整原值，打码时保留目录 ID 便于识别同账号的多个 Key
- 成功率统一保留两位小数
- 抽取 `eventStream` 模块，对话测活与网关统计共用同一份 AWS event-stream 帧解析

### 修复

- **修复社交登录（Google / GitHub）授权成功后回调被 Kiro IDE 接走**，
  本应用永远等不到授权码。根因是打包配置没有声明 `kiro` 这个 URL Scheme，
  而 macOS 只允许把 Info.plist 中已声明的 scheme 设为默认处理者，
  导致登录期间的临时接管静默失效；同时补上抢占失败时的明确提示
- 修复网关调用历史出现「成功数大于请求数、失败数为 -1、成功率 150%」的问题。
  根因是请求按发起时间归桶、结果按响应时间归桶，而流式对话经常跨过分钟边界；
  现在改为响应回来后按请求发起时间一次性写入
- 修复自动重试时中间失败未被计数，导致累计请求数与成功 / 失败数对不上
- 修复网关积分统计恒为 0：上游响应可能是压缩的，而帧解析拿到的是压缩字节，
  现在按 `content-encoding` 解压后再解析
- 修复网关旁听响应流时可能吃掉开头数据块的隐患，改用独立的 PassThrough 分流
- 修复添加账号弹窗在授权失败时被上游的长 JSON 错误顶破布局
- 移除 token 统计：该协议的响应流不返回 `tokenUsage`，取不到有效数据，
  改为只统计积分

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.10] - 2026-08-04

本版本修掉一个在旧版 Kiro 上「提示成功但实际没生效」的网关缺陷，并让用量刷新学会跳过
已经确定失效的账号与 Key，不再把时间花在必然失败的请求上。

### 新增

#### Kiro 版本能力探测

- 新增 Kiro 版本能力探测：判据是「kiro-agent 扩展是否真的读取 krsEndpoints / cpsEndpoints」，
  而不是比较版本号大小。流式扫描扩展文件（40MB 以上不整体读入内存），按文件体积与修改时间
  缓存结果，IDE 升级后自动失效
- 跨平台定位 Kiro 安装目录：macOS、Windows、Linux 各自的常见安装路径都会尝试；
  探测不到时按「支持」放行，避免因为路径没覆盖到就误拦本来能用的功能
- 开启网关前做硬校验，不支持的版本直接拦下并说明原因，不再写出一份 IDE 根本不看的配置
- API Key 管理页检测到不支持的版本时显示醒目提示，开启网关与切换 Key 都会弹窗告知需要升级，
  并提供 Kiro 官网下载入口

#### 模型倍率

- 账号测活与 API Key 测活的模型下拉新增消耗倍率显示，如 `Claude Opus 5（claude-opus-5） 1x`，
  取自上游 Model 结构里的 `rateMultiplier`
- 倍率一并拼入选项文本，搜索模型时也能按倍率匹配；上游未返回倍率的模型（如 auto）自动省略

#### API Key 详情

- 新增 User ID 展示，取自 `Get-Usage-Limits` 响应的 `userInfo.userId`，随刷新用量一起更新。
  同一账号签发的多个 Key 会共用同一个 User ID，可据此识别「这几个 Key 其实是一个号」
- User ID 跟随全局隐私打码，打码时保留目录 ID 与 UUID 首段，便于在打码状态下比对归属
- 邮箱与 User ID 都新增复制按钮，复制得到的始终是完整原值，与界面是否打码无关

### 优化

- 用量 / 积分刷新新增跳过策略：凭证被拒、403、账号封禁这类**重试也不会变好**的确定性失败
  会被跳过；限流、5xx、超时、网络故障等临时故障一律继续重试，不会因为一次网络抖动
  就让账号或 Key 永久停在异常状态
- 跳过策略统一收敛到 `shared/refreshPolicy`，账号自动刷新、账号手动批量、
  API Key 自动刷新、API Key 手动全量四条路径共用同一份判定，不会再出现
  「手动跳过了、自动还在刷」的口径漂移
- 勾选后刷新与卡片上的单个刷新按钮**不跳过**：用户已明确指定目标，异常项始终留有手动重试入口
- 刷新结果提示带上跳过数量，全部被跳过时明确说明原因，不再静默什么都不做
- 账号管理与 API Key 管理的刷新按钮文案统一为「刷新用量/积分」；
  刷新过程中显示实际处理的条数，如「正在刷新 5 个账户用量/积分...」
- API Key 详情抽屉加宽（窄窗口下按视口比例自适应），完整凭证、邮箱、User ID 等长文本
  改为按字符换行而非截断，操作按钮顶部对齐，多行时不再错位
- 上游返回的整段 JSON 错误（内含无空格长 URL）现在会正确断行，不再溢出提示框

### 修复

- 修复在旧版 Kiro（如 0.11.133）上开启 API Key 网关时「弹窗提示写入成功、实际请求仍走官方额度」
  的问题。根因是旧版 Kiro 仍使用 CodeWhisperer 端点体系（`q.<region>.amazonaws.com`），
  既不读取 `codewhisperer.config.krsEndpoints` / `cpsEndpoints`，也不会发出可被接管的
  KRS / CPS 请求，而写入后的回读校验只能证明文件写对了、证明不了 IDE 会采用这两个键
- 修复 API Key 批量刷新的进度总数把被跳过的 Key 也算进去，导致进度条差着几个刷不满、
  看起来像卡住的问题

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.9] - 2026-08-01

本版本把订阅档位理清：Enterprise 不再占用一个订阅档位（它是登录方式），Power 独立成档，
并补上 Pro Max。API Key 管理页新增与账号管理同构的筛选面板，
同时修掉一个会让列表整个变空的筛选缺陷。

### 新增

#### API Key 筛选
- API Key 管理页新增筛选面板，四个维度：订阅档位、状态（正常 / 异常 / 未检查）、
  使用量占比、额度重置剩余天数
- 每个选项带命中计数，工具栏按钮上有角标显示生效条件数，
  统计行另有「筛选中 N 项」标签可一键清空
- 与账号管理的筛选面板同构，两者外观样式收敛到同一处，不再各写一份
- 卡片补充显示额度重置日期，位置与账号卡片一致。上游返回额度的同一个接口本来就带重置时间，
  此前 API Key 侧没有解析，因此这个字段需要重新同步一次才会有值

#### 添加账号弹窗的导入入口
- 「添加账号」弹窗在三种添加方式下方新增「从文件导入」与「输入 JSON 导入」，
  点击后关闭当前弹窗、直接打开对应的导入弹窗，不必退出去再找工具栏的「导入」下拉
- 两组入口用分组小标题隔开：上面是新增单个账号，下面是批量导入已有账号

### 修复

- **订阅档位里混进了登录方式**：Enterprise 是登录方式（IdC），不是订阅档位，
  两者是正交的 —— 一个账号完全可以「订阅 Power、用 Enterprise 登录」。
  此前的判定把标题里的 ENTERPRISE 当档位，还把 POWER 一起并进这一档，
  结果 Power 账号既显示不出自己的档位，也没法单独筛选
- **Power 档位筛选恒为 0**：`subscription.type` 是从上游标题派生出来的持久化值，
  只改判定逻辑不会动到磁盘上已存的旧值，除非把每个账号的用量重新刷一遍。
  现在主进程在读取账号数据时按标题重新对齐档位并落盘，启动一次即生效
- **范围筛选清空后列表变空**：在「使用量」或「重置剩余」里填过数字再删空，
  列表会变成空白。`a-input-number` 在内容被删空时给出的是 `null`，而筛选按
  `!== undefined` 判断「是否已设置」，`null` 因此被当成有效条件，比较时又被转成 `0`，
  于是 `剩余天数 > 0`、`用量 > 0` 把几乎所有条目都滤掉了。
  现在范围条件统一按 `!= null` 判断，输入框也统一收敛成未设置，
  清空后命中数量与列表实时恢复；筛选按钮角标此前同样会把清空的输入算作一项，一并修正

### 优化

#### 添加账号弹窗
- 遮罩点击关闭改为按步骤区分：停在「选择添加方式」时点遮罩可以关闭，
  进入在线登录之后（选择登录方式 / Enterprise SSO / 等待浏览器授权）不再响应遮罩点击，
  点返回退回到「选择添加方式」后又可以关闭。
  此前所有步骤一律禁止点遮罩关闭，连什么都没做的第一步也得去点右上角
- OIDC 凭证那一步同样不响应遮罩点击：该步骤有填了一半的 Refresh Token 表单，
  误触遮罩会全部丢掉

### 变更

- 订阅档位新增 Pro Max，档位顺序为 Free / Pro / Pro+ / Pro Max / Power / Teams。
  判定链对顺序敏感：Pro Max 与 Pro+ 都含子串 “Pro”，两者必须先于 Pro 判断
- 档位标签配色：Power 用黄色（Enterprise 不再占用档位后腾出来的颜色，
  与 API Key 页原本的 Power 配色一致），Pro Max 用洋红
- 订阅档位的判定收敛到一处共享实现，主进程解析接口响应与读取历史数据时对齐都走它，
  不再各持一份正则
- API Key 的档位配色改为与账号管理共用同一张表，同一个订阅在两个页面颜色一致；
  档位判定也改为直接取上游标题，不再依赖旧的小写分层字段（那套分层没有 Pro Max，
  且把 Pro Max 并进了 Pro+）
- 订阅档位的展示统一走带兜底的查表函数：磁盘上可能残留已废弃的档位值，
  直接索引元数据表会取到 `undefined`，进而让整张卡片渲染失败

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.8] - 2026-07-31

本版本新增 API Key 批量测活，并修好一个让人误判的显示问题：被封禁的 Key 此前在列表里
一直显示「正常」。同时把两个管理页的工具栏按钮改为带文字，批量刷新时按钮上能直接看到进度状态。

### 新增

#### API Key 批量测活
- API Key 管理页工具栏新增「批量测活」：有勾选就只测勾选的，没勾选则测当前列表全部。
  目标与顺序都取自界面上可见的卡片，被搜索或筛选排除的 Key 不会被带进来
- 逐个串行执行而非并发：批量并发容易被上游判为异常活动，反而污染检测结果
- 弹窗内实时显示每个 Key 的测活状态与耗时，顶部汇总可用 / 失败 / 未测数量，
  进度会自动把当前项滚进视野并多露出后续几项，中途可随时中止
- 失败原因保留接口返回原文，过长时截断，悬停查看完整内容
- 关于额度消耗：只有真正可用的 Key 才会出字并计一次请求，提示词已压到最短；
  已失效或被封禁的 Key 会被上游即时拒绝、不产出内容，因此不计费。弹窗内对此有明确说明
- 模型交给 Kiro 自行挑选，不必为每个 Key 先拉一遍模型列表

### 修复

- **被封禁的 API Key 在列表里显示「正常」**：管理面的 Get-Usage-Limits 与
  List-Available-Models 都不校验账号状态，被封禁的 Key 在这两个接口上照样返回 200，
  只有 runtime 面的真实对话才会暴露 403。而测活结论若写进 `lastError`，
  下一轮自动刷新同步成功就会把它清掉。现在测活结论单独存在 `lastChatError` 里，
  卡片状态、详情抽屉与正常 / 异常计数均取两者的并集，不会再被自动刷新擦掉
- 测活结论由主进程落库后界面同步更新，不必为每个 Key 再往返一次 IPC 重载整表
- 用户主动中止的测活不写入结论：中止不构成检测结果，不该据此把 Key 标成异常
- Key 换区后一并清空上一区查得的测活结论，避免跨区展示陈旧状态
- **接管面板与删除确认里的 API Key 无视隐私打码开关**：这两处用的是页面内另一份
  无条件打码的实现，关掉「隐私打码」也只能看到星号。现已统一走全局开关，
  并删掉与 `utils/display` 重复的那份实现

### 优化

#### 批量刷新
- 账户管理的「刷新密钥」「刷新用量」在刷新期间不再只是置灰，而是显示加载态与
  「正在刷新密钥...」「正在刷新用量/积分...」；自动刷新与手动批量刷新共用同一条链路，
  两种触发方式都会反映到按钮上
- API Key 管理的刷新按钮此前只在手动点击时有加载态，自动刷新那一轮按钮是静的，
  现在改读全局同步状态，两种触发都显示「正在刷新 API Key 用量/积分...」
- 有勾选时按钮上带出条数（形如「刷新密钥（3个）」「刷新用量（3个）」「刷新（3个）」）。
  账户管理这两个按钮一直是「有勾选就只刷勾选的账号」，但此前按钮上看不出范围，
  容易误以为会刷全部；条数取当前筛选结果里的勾选数，与实际处理的条数一致
- API Key 管理的批量刷新改为与批量测活共用同一套目标口径：勾选项只取界面上可见的那些。
  此前是从全部 Key 里筛勾选项，勾选后再搜索，被搜索排除的 Key 仍会被刷到

#### 工具栏
- 账户管理与 API Key 管理的工具栏按钮由纯图标改为图标 + 文字（刷新密钥 / 刷新用量 /
  隐私打码 / 删除、刷新 / 隐私打码 / 批量测活 / 删除），
  不必悬停等 Tooltip 才知道每个按钮是做什么的
- 「隐私打码」按钮在开启时文案变为「隐私打码中」，状态一眼可辨
- 移除账户管理工具栏的「退出登录」按钮：账号卡片上已有同一个入口，
  而且两处执行的动作完全相同（都是清空整个 `~/.aws/sso/cache` 目录，与选中哪个账号无关）
- 两个页面的「删除」按钮改为只在有勾选时出现（此前是常驻置灰），并带出条数（形如「删除（3个）」）。
  删除作用于全部勾选项、不受当前搜索影响，因此这里的条数取全部勾选数，与确认弹窗里的数字一致

#### 删除确认
- 确认按钮文案由「删除」统一为「确认删除」，并改为实心红色。
  Ant Design 的 `okType: 'danger'` 渲染出来是红色描边，在弹窗里与「取消」区分度不足
- 账号与 API Key 的单个删除、批量删除四处确认弹窗收敛到同一个 `confirmDelete`，
  文案与按钮样式不再各写一份

### 文档
- README 标语与 `package.json` 描述改为「Kiro 多账号管理器轻量版」

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.7] - 2026-07-31

本版本给积分变化日志加上导出能力：一键把某个账号或 API Key 的全部积分记录存成 Excel 表格。

### 新增

#### 积分变化导出
- 积分变化弹窗的工具栏新增下载按钮，选好保存位置即导出为 `.xlsx` 表格，
  账号与 API Key 两处的积分变化弹窗都能用
- 导出的列与界面表格一致（时间 / 已用积分 / 总额度 / 变化 / 占比 / 额度构成），
  行序同样是最新的排在最上面，所见即所得
- 保存对话框的文件类型补上「Excel 工作簿」

### 优化

#### 导出体验
- 表格里写的是原始数值而不是格式化后的文本：时间是真正的日期时间、积分是数值、
  占比是百分比单元格，在 Excel 里可以直接排序、求和、透视、画图；
  小数位跟随设置里的「积分两位小数」
- 预置冻结首行、自动筛选与列宽，几百条记录打开就能看，不用先调格式
- 文件名自动带上主体名与精确到秒的时间戳（形如 `kiro-usage-<名称>-20260731-162337.xlsx`），
  同一天多次导出不会互相覆盖；纯中文或已打码的名称会退化成只留时间戳
- xlsx 由主进程手写 OOXML 后自行打包，没有引入任何表格库，安装包体积不受影响

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.6] - 2026-07-31

本版本重做账号导入入口并支持一次导入多个文件，把 API Key 的区域从全局设置下沉到每个 Key
自己携带，重写了网关接管的检测方式（不再因 Kiro IDE 回写配置而误报异常），
并修正了安装包被打入构建期依赖导致体积虚高的问题。

### 新增

#### 账号导入
- 导入入口改为下拉菜单，拆分成「从文件导入」和「输入 JSON 导入」两个独立弹窗，
  各自只保留自己需要的表单
- 从文件导入支持拖拽或点选，一次可选多个 JSON 文件，按列表顺序依次导入并汇总为一份结果，
  不必为每个文件反复打开弹窗
- 两个导入弹窗都改为居中显示，文件拖拽区域放大并让内容垂直居中

#### API Key 区域
- 添加与导入 API Key 时单独选择所属区域，替代原来「一个区域管全部 Key」的设置，
  不同来源的 Key 可以分属不同区域
- 已添加的 Key 可在卡片和详情抽屉里随时换区，换区后自动重新同步订阅与额度，
  并清空按旧区域查得的积分历史，避免展示跨区的陈旧数据
- 卡片与详情抽屉显示该 Key 绑定的注册邮箱（同步时向上游查回，未返回时占位）

### 优化

#### 网关接管检测
- 接管状态改用「settings.json 端点已绑定」或「本次网关会话近期确实有请求经过」两者任一成立来判定。
  此前只看配置文件，一旦 Kiro IDE 启动时按自身内存把端点回写清空，
  即使请求仍在正常转发也会误报「IDE 接管状态异常」，并连带认不出实际在用的 Key
- 新增端点守护：接管期间监视 settings.json，端点被外部改写就自动改回本地网关。
  此前被回写后磁盘配置即已丢失，IDE 下次重启就会直连官方端点、静默失去接管
- 开启网关时默认使用列表最上方的第一个 API Key。此前取的是存储顺序里最早添加的那个，
  与界面排序不一致，确认弹窗里显示的 Key 和实际启用的对不上
- 移除 API Key 管理页下方一连串网关状态提示条

#### 安装包体积
- 修正 ant-design-vue、@ant-design/icons-vue、uuid 被登记为运行时依赖的问题。
  三者只在构建期使用、早已打进渲染层产物，却被一并塞进安装包：
  app.asar 从 89 MB 降到 7.7 MB，安装包体积减少约 80 MB，启动时的校验与读取开销同步下降

#### 界面
- 关于页改为介绍本应用与主要功能，顶部只保留居中的 Logo 与应用名，
  并在「加入交流群」旁增加 GitHub 仓库入口
- API Key 详情抽屉重排：凭证表格移到基本信息上方并补充邮箱行，移除积分进度条，
  复制按钮与区域修改按钮在表格内右侧对齐，底部固定「切换到该 Key」
- 错误提示改为直接显示接口返回内容，不再套用中文文案掩盖真实原因，悬停可用 Tooltip 查看完整信息
- 添加账号弹窗把昵称与注册密码单独排一行，各输入框补齐占位提示
- 路径展示不再省略中间部分，复制按钮旁增加「打开所在目录」
- 收紧表单标题的下边距，改善各页面标题与控件之间过大的间隔

### 文档
- README 在设置页截图上方补充常用工具页与系统日志页截图

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.5] - 2026-07-30

新增「常用工具」页面，首个工具是「自动同意 AI 操作（命令 / 文件 / 网络）」：把 Kiro 执行
终端命令、读写文件、发起网络请求与搜索时逐条弹出的确认框一次性放行。

### 新增

#### 常用工具

- 新增「常用工具」页面与侧边栏入口
- 新增「自动同意 AI 操作（命令 / 文件 / 网络）」开关：开启后 Kiro 执行终端命令、读写文件、
  发起网络请求与网络搜索都不再逐条弹出确认框。默认关闭，开启前需在弹窗中二次确认
- 同时写入两套机制以覆盖不同 Kiro 版本：
  - Kiro 0.x 走 IDE 设置 `kiroAgent.trustedCommands`，向白名单加入通配项 `*`，
    并临时清空优先级更高、按子串包含匹配的 `kiroAgent.commandDenylist`（仅对终端命令生效）
  - Kiro 1.0+ 走 `~/.kiro/settings/permissions.yaml`，追加不带 `match` 的
    `shell`、`fs_read`、`fs_write`、`web_fetch`、`web_search` 五条 allow 规则，
    命中「无条件放行」判定后跳过后续匹配（文件与网络类能力只能由这套机制覆盖）
- 两处均为追加写入而非重写：用户原有条目、注释与格式一字不动；开启前的原值完整备份，
  关闭开关时逐字还原（原本没有的键或文件会被删除），备份丢失时只移除本应用写入的项
- Kiro 实时读取设置并监听权限文件，因此开关**无需重启 Kiro** 即时生效
- 开启前弹窗明确风险：无条件放行同样覆盖 `rm -rf` 等破坏性命令，以及任意文件读写和任意
  网址请求；Kiro 内置规则中没有针对这些能力的兜底拦截，且开启期间命令拒绝名单会被临时清空
- 页面逐项展示两套机制的目标路径与生效状态，并识别无法安全处理的情况：
  未检测到 Kiro 用户数据目录、仅存在 `permissions.json`（YAML 会抢占优先级）、
  YAML 缺少 `rules` 顶层键；同时提示 `deny` 规则导致放行失效、以及用户自行配置的放行规则

### 优化

- ant-design-vue 改为按需自动引入（`unplugin-vue-components` + `AntDesignVueResolver`），
  不再全量 `app.use(Antd)`：组件包体积从约 1.2MB 降到约 785KB，减少打包版首屏脚本解析开销
- macOS DMG 安装窗口重新布局：顶部放「安装指南.txt」，下方左侧应用、右侧「应用程序」，
  中间保留拖拽安装的引导

### 文档

- macOS DMG 内新增「安装指南.txt」：说明首次打开被 Gatekeeper 拦截属正常现象及放行办法
  （右键打开、`xattr -cr` 处理「已损坏」提示），避免用户把安全拦截误当成安装失败
- 安装说明补充卸载前需要还原的 Kiro 配置项，以及已卸载后手动还原
  `settings.json` 与 `permissions.yaml` 的方式
- 安装说明新增「开关已打开但仍会弹确认框」的排查条目，并合并原本重复的
  「升级与卸载」「常见问题」两处章节

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.4] - 2026-07-28

本版本新增完整的 Kiro API Key 管理与本地网关接管能力，并继续完善积分变化、
更新提醒和低窗口高度下的桌面端体验。

### 新增

#### API Key 管理与本地网关

- 新增 API Key 管理页面：支持单个添加、批量导入、搜索、排序、全选、批量删除、
  导出、备注编辑、完整 Key 复制及隐私打码
- 新增 API Key 订阅与额度同步：展示订阅等级、已用 / 总额度、用量百分比和分段柱状条，
  支持单个与批量刷新；异常 Key 同样保持完整的 `0 / 0` 用量布局并展示红色错误提示
- 新增 API Key 真实测活：获取可用模型并发送一次真实对话，验证凭证可用性
- 新增 API Key 积分变化历史：记录每次额度采样，提供趋势图、明细表、刷新和清空操作，
  与账户积分历史使用同一套交互
- 新增本地 KRS / CPS 网关：开启后将 Kiro IDE AI 请求接管到本地代理，
  支持 Region 与端口配置、端口冲突检测、端点写后校验和关闭时原样恢复
- 支持在网关运行期间切换当前 API Key，下一次请求立即读取新 Key；
  开启网关并使用指定 Key 可在一次操作中完成
- 新增网关实际请求观测：区分“已选择 Key”“等待 IDE 请求确认”和“当前实际使用 Key”，
  并可手动重新检测

#### 更新体验

- 应用冷启动后静默检查 GitHub 最新 Release；检查失败不弹错误、不阻塞应用启动
- 发现新版本时自动弹出全局更新模态框，展示版本变化、发布时间和 Markdown 更新说明
- 侧边栏 Logo 新增“新版本”徽章，点击可重新打开更新弹窗
- 更新结果写入本地缓存，6 小时内直接复用；当前应用版本变化后旧缓存自动失效
- 关于页手动“检查更新”始终绕过缓存，确保用户主动检查时读取最新结果

### 优化

- API Key 网关开关改为带图标的“开启网关 / 关闭网关”按钮，关闭状态使用实心红色危险确认按钮，
  开启和关闭均增加二次确认
- API Key 卡片不再使用“登录 / 退出登录”语义，统一为“当前使用 / 切换到该 Key”；
  正常 / 异常状态标签移动到订阅等级左侧，为 Key 文本释放更多横向空间
- 关闭网关时同步清除当前 Key；网关关闭状态不存在“当前使用”Key；
  点击顶部“开启网关”会自动使用列表中的第一个 API Key，列表为空时提示先添加
- 网关未开启时点击切换 Key，会提供“开启网关并使用该 API Key”的组合操作，
  成功后继续显示重启 Kiro IDE 弹窗
- API Key 页面新增与账户管理一致的显隐图标，并与全局隐私打码设置联动
- 账户与 API Key 共用的积分变化弹窗改为居中展示，图表和表格设置最低高度；
  小窗口下由弹窗外层滚动，避免表头、表体和分页被压缩错位
- API Key 卡片统一标签行、用量模块和错误提示槽高度，正常、异常和未同步状态保持对齐

### 修复

- **主动续期成功但未同步 IDE 时会丢弃新凭证**：刷新一旦成功，服务端就已轮换掉旧
  refreshToken，而原先在「未写入 IDE」的分支直接 return，新凭证既没落盘也没下发给界面，
  留在内存与磁盘里的旧值立即作废，下一次刷新必然 `invalid_grant` /
  `HTTP 401 Bad credentials`。现在无论是否同步成功都先持久化并下发新凭证
- 修复账号可能陷入「主动续期已停、自动刷新又把它排除」的空档：续期因该账号不再是 IDE
  激活账号而停止调度时，同步修正 `isActive`，交回给自动刷新覆盖，不再等到 token
  过期才被动刷新
- 刷新 Token 遇到凭证被拒时，若该账号确实是 IDE 当前登录账号，改用磁盘上的最新
  refreshToken 重试一次，解决 Kiro IDE 自身 refresh loop 抢先轮换导致的刷新失败；
  登录方式或 provider 对不上时不会误用别的账号凭证
- 同一账号的刷新改为并发去重：手动刷新、自动刷新与主动续期同时命中时复用同一次请求，
  不再出现「先到的成功轮换、后到的拿作废旧值报 401」
- 凭证失效与网络异常在界面上区分开：前者状态标记为「已过期」并提示重新登录，
  不再直接把 `{"message":"Bad credentials"}` 这类原始响应体摊在卡片上
- 修复网关状态异步加载可能用旧快照覆盖主进程最新推送，导致标题栏误报“IDE 未由本应用网关接管”
- 修复已开启网关但尚未产生真实 IDE 请求时无法显示已选择 API Key 的问题，
  现在会明确提示“等待 IDE 请求确认”
- 修复关闭网关后当前 Key 状态仍残留在卡片、详情和标题栏的问题
- 修复积分变化弹窗在软件高度较低时表格高度坍缩、表头与内容错位的问题
- 修复异常 API Key 使用独立占位块导致卡片内部高度和操作栏位置不一致的问题

### 文档

- 补充 `INSTALL.md` 安装说明：各平台安装步骤、macOS Gatekeeper 与 Windows SmartScreen
  放行方式、Linux FUSE 依赖、升级与数据保留、卸载与常见问题
- 每个版本的更新日志底部新增安装说明入口，README 安装章节同步给出链接
- README 补充 API Key 管理、API Key 网关与关于页截图

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.3] - 2026-07-27

围绕「打包版看起来不会自动刷新」做的一轮排查与整改：问题出在打包产物没有任何日志可看，
而不是刷新机制本身。顺势补上了内置的系统日志页面，以后排查不再依赖 DevTools。

### 新增

#### 系统日志

- 新增「系统日志」页面：时间倒序的日志流，支持按关键字（消息 / 分类）搜索、
  时间范围、分类、级别（DEBUG / INFO / WARN / ERROR）多维筛选，
  并可选择显示行数上限（500 / 1K / 5K / 20K）
- 日志中心采用内存环形缓冲 + 分片落盘：内存固定保留最近 5 万条，写入 O(1)、占用有上限；
  磁盘按 5000 条一个分片、最多保留 10 份，避免出现巨型日志文件；写盘按 800ms 批量 flush，
  不会让高频日志把主进程 IO 拖满
- 接管主进程 console：现有代码里的 `console.log('[Net] ...')` 自动按 `[分类] 消息`
  归类入库，无需逐个改调用点，终端输出照旧
- 渲染进程日志经 IPC 汇入同一个日志中心，主进程与界面的日志在同一条时间线上
- 工具栏提供自动跟随最新日志、手动刷新、导出当前筛选结果为 `.log`、
  打开日志目录、清空日志（同时删除磁盘分片）
- 日志列表用虚拟滚动渲染，两万行也只维持视口内的 DOM
- 关键链路补充日志埋点：接口请求与响应码、批量刷新结果、自动刷新每轮的账号数与耗时、
  失败明细

#### 检查更新

- 关于页新增「检查更新」：读取 GitHub Releases 的最新版本号与当前版本做语义化比较，
  已是最新弹「已是最新版本」，有新版则弹出当前版本 → 最新版本、发布时间与更新说明，
  可一键跳转 Releases 页面下载。请求走统一网络层，设置里的 HTTP 代理对它生效
- 更新说明按 Markdown 渲染（marked + DOMPurify 净化），正文里的链接一律交给系统浏览器打开
- 关于页新增「加入交流群」：弹窗展示 QQ 群二维码

### 修复

- **打包版丢失全部渲染进程日志**：生产构建的 terser 配置是 `drop_console: true`，
  会把渲染进程所有 `console.*` 调用整个删掉，`[AutoRefresh]` 之类的日志在打包版里根本不存在，
  自动刷新到底有没有执行完全无从判断。现改为只删调试性质的 `log` / `debug` / `trace`，
  保留 `info` / `warn` / `error`
- 主进程新增渲染进程日志转发：把 `[Xxx]` 前缀的日志与警告以上级别转发到主进程 stdout。
  打包后的应用没法随手开 DevTools，这是排查后台任务问题的唯一通道
- 日志页切换级别筛选时报 `An object could not be cloned`：`levels` 取自 ref 里的数组，
  本身是响应式代理，经 IPC 结构化克隆会失败。查询条件统一过 `toPlain` 剥掉代理，
  查询与导出两条路径一并修好。该错误还会被日志系统捕获后再写进日志、触发下一轮刷新，
  形成自我循环
- 导出文件被追加多余扩展名：保存对话框的类型筛选写死且 JSON 排在首位，
  macOS 会按第一项补扩展名，导出日志会变成 `xxx.log.json`。改为按文件名扩展名动态排序，
  匹配项置顶，同时补上 `.log` 类型；CSV / TXT 导出的同类问题一并修复

### 变更

- **移除 JavaScript 混淆链路**：删除 `scripts/javascript-obfuscator.mjs` 与
  `obfuscate` 脚本，`prepack:dist` 不再执行混淆，同时卸载 `javascript-obfuscator`、
  `cli-table3`、`dotenv` 三个 devDependencies，并删除仅为混淆脚本提供 `VITE_OUTDIR` 的
  `.env.production`。
  原因：混淆让产物体积膨胀约 219%，却使打包版彻底不可诊断；实测强档配置对并发池等
  核心逻辑没有语义影响，保留它的性价比不足
- `LICENSE` 从建仓时误留的 Apache-2.0 全文替换为 AGPL-3.0 官方文本，
  与 `package.json` 的声明一致（GitHub 的许可证识别按文件内容判定）
- 虚拟列表新增动态行高模式：行高先按估算值渲染，再用 `ResizeObserver` 实测可见行并按
  key 缓存，位置改用前缀和 + 二分查找定位。日志因此可以自动换行，长消息（失败明细、
  整段 JSON）不再需要横向拖动。定高模式的原有 O(1) 计算路径保持不变
- 关于页头部改为居中的品牌横幅：应用标识、「Kiro 账户管理器」标题、版本号与
  检查更新 / 加入交流群两个入口
- 日志页工具栏的下拉与按钮尺寸统一为 large，与搜索框一致；时间、分类下拉加宽，
  等级筛选按钮高度对齐
- 日志导出文件名改用与账号导出一致的时间戳格式（`kiro-logs-20260727-115749.log`），
  精确到秒且带年份

### 说明

自动刷新机制经实测是正常工作的：一轮覆盖 135 个账号耗时约 15 秒，冷启动补跑也会立即执行。
此前观察到的「没刷新」，实际是部分账号的接口返回 `HTTP 400 Improperly formed request.`
或 `HTTP 403 bearer token invalid`，这些账号的数据不会更新，需要单独处理凭证问题。

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.2] - 2026-07-27

重点修复自动刷新时不时不生效的问题，根因是 refreshToken 的轮换在主进程与渲染进程之间没有同步。

### 修复

- **主动续期结果无人接收**：主进程续期成功后发出的 `proactive-renewal:done` 没有任何订阅方，
  渲染进程内存里仍是轮换前的 refreshToken，而任何一次 `persist()` 都会全量写盘，
  把作废的旧值覆盖回磁盘，之后再刷新必然 `invalid_grant`。
  现已补上 preload 通道与渲染进程订阅，续期后立即同步内存凭证
- **用量刷新轮换凭证后不通知任何人**：accessToken 过期（401）时 `checkAccountStatus`
  会顺手刷新一次并轮换 refreshToken，但既不写入 IDE 的 token 文件也不重排主动续期。
  结果是 IDE 手里的凭证作废，且主动续期因「磁盘 refreshToken 对不上」判定不是激活账号后
  停止调度。现已抽出 `syncCredentialsToIde` 供两处复用，轮换后同步写盘并重排续期
- **批量并发池吞任务**：并发池内单个账号抛异常会中断所在通道，该通道排队中的账号被整批丢弃，
  表现为「一批里有些账号没刷新」。现已就地兜住异常并计入失败
- **密钥刷新与主动续期争抢同一账号**：IDE 当前激活账号在「主动续期」开启时不再参与批量密钥刷新，
  交由主进程独占处理，避免双方先后拿着同一个旧 refreshToken 互相作废
- **刷新结果被旧引用覆盖**：`refreshToken()` 在请求返回后用的是请求发起前捕获的 credentials，
  会覆盖掉这期间主动续期写入的新值，现已重新取最新引用
- 未开启「积分两位小数」时积分显示为整数：原先是「最多两位小数」，
  `8586.94` 这类值看起来像开了小数

### 优化

- **冷启动补跑**：上次跑完的时间记录到 localStorage，启动时若距上次刷新已超过间隔，
  立刻补跑一轮而不是干等一个完整间隔；把刷新间隔从长调短、或重新打开开关时同样适用
- **修正间隔漂移**：原先每轮执行完都按「当前时间 + 间隔」重新计时，实际周期变成
  `间隔 + 本轮耗时`，账号多时越跑越晚。现在只在本轮耗时确实超过一整轮时才顺延
- 自动刷新补充控制台日志（本轮账号数、成功/失败数、整轮耗时、失败明细），便于核对间隔是否准确
- 「邮箱打码」更名为「隐私打码」，说明改为「列表与详情中隐藏邮箱、昵称等隐私信息」，
  与实际行为一致（昵称通常就是邮箱前缀，一直是一起遮罩的）

### 文档

- README 新增安装说明：三平台安装包对照表、macOS 未签名的两种放行方式、
  Windows SmartScreen 提示、Linux AppImage 缺少 FUSE 的处理
- 更新应用 logo

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.1] - 2026-07-26

界面细节与交互优化，无功能变更。

### 优化

- 主页四张统计卡片悬停时改为浅主题色底 + 主题色描边，替换原先偏重的阴影反馈，
  底色用 `color-mix` 与卡片底色混合，深浅模式与自定义主题色都能自然跟随
- 「需要关注」列表项的悬停底色原先写死为默认紫，换主题色后与其它区域不一致，现已跟随主题色
- 统计卡片网格由 `auto-fill` 改为 `auto-fit`，宽屏下卡片拉伸铺满整行，不再于右侧留出空轨道
- 抽出 `--kal-hover-shadow` 变量统一可点击卡片的悬停阴影，深浅模式各一档

### 修复

- 账户管理页滚动时偶发跳回顶部：回到顶部的 `watch` 源是每次求值都新建的数组，
  且依赖了排序结果长度，自动刷新替换账号数据后计算属性重算即触发回顶。
  改为「筛选条件 + 排序方式」的值快照，只有条件真正变化才回到顶部
- 同时修正一处判断口径：筛选条件数量不变但内容变化时（如从「只看正常」切到「只看异常」）
  原先不会回顶，现已正确触发

### 文档

- 修正 README 中「更新日志」「交流群」两处标题的乱码字符

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

## [1.0.0] - 2026-07-26

首个正式版本。基于 [Kiro-account-manager](https://github.com/chaogei/Kiro-account-manager)（AGPL-3.0）的接口实现，
用 Vue 3 + Vite + Pinia + Ant Design Vue + Electron 重写，并裁剪为纯账户管理工具。

### 新增

#### 账号管理

- 账号列表：邮箱 / 昵称 / 备注关键字搜索，按状态、订阅、登录方式多选筛选，支持列排序
- 账号详情抽屉：订阅类型、积分明细（基础 / 试用 / 奖励额度）、超额费率、Token 有效期、凭证查看与复制
- 单个与批量删除，删除前二次确认可在设置里开关
- 积分变化日志：按账号记录每次用量采样，可查看与清空

#### 添加账号

- 在线登录：Google / GitHub 社交登录（PKCE + `kiro://` 回调）、AWS Builder ID 设备码、
  Enterprise IAM Identity Center SSO（授权码 + PKCE + 本地回调端口），三条链路均支持无痕窗口
- OIDC 凭证：手动填写 Refresh Token / Client ID / Client Secret
- 本地 Kiro 凭证：直接读取 Kiro IDE 当前已登录的账号

#### 切号与刷新

- 切换账号：写入 `~/.aws/sso/cache/kiro-auth-token.json`，写盘前强制刷新一次 Token，
  避免 IDE 拿到已作废的 Refresh Token 被强制登出
- 切号结果弹窗内可一键重启 Kiro IDE，让它重新读盘生效
- 刷新密钥：单个 / 批量刷新 Access Token，仅当账号确实是 IDE 当前登录账号时才回写磁盘
- 刷新积分与用量：单个 / 批量拉取最新订阅与积分，并发数可配
- 主动续期：按 Token 到期时间提前调度刷新，保持 IDE 登录态不掉线
- 自动刷新：密钥与用量各自独立的开关与间隔，窗口最小化到托盘后定时器不被节流

#### 账号测活

- 拉取账号真实可用的模型列表，发起一次真实流式对话验证可用性
- 流式输出实时回显，支持中途取消

#### 导入与导出

- 导入：卡密（`邮箱----密码----Token----ID----Secret----登录方式`）、精简 JSON 数组、
  完整备份 JSON、CSV、TXT，支持粘贴文本或选择文件，大批量走并发校验池
- 导出：完整备份 JSON、精简 JSON、卡密、CSV、TXT，可保存文件或复制到剪贴板，
  并可选择是否包含凭证

#### 桌面端体验

- 系统托盘（macOS 菜单栏）常驻：显示当前账号摘要，支持刷新、切换到下一个账号、
  复制当前邮箱、显示主窗口、退出程序
- 关闭窗口行为可配：最小化到托盘 / 直接退出 / 每次询问；macOS 隐藏窗口时同步隐藏 Dock 图标
- macOS 中文菜单栏：应用名菜单内「关于 Kiro Manager Lite」弹出原生信息弹窗（支持一键复制），
  「操作」菜单提供主页、账户管理、设置、关于四个页面跳转与打开 / 关闭 Kiro IDE
- 自定义协议 `kiro-manager-lite://` 与短别名 `kml://`：从浏览器或终端唤起应用并直达指定页面
- 单实例锁：重复启动时唤起已有窗口，并处理随命令行传入的协议 URL

#### 设置

- 外观：深色模式、主题色、邮箱打码、积分显示精度
- 性能：批量操作并发、导入校验并发
- 网络：用量接口类型（REST / CBOR 门户）、HTTP 代理开关与地址（留空回退系统环境变量）
- 数据：存储文件与备份目录一键打开，支持恢复默认设置

### 安全

- 账号凭证保存在本机 electron-store 加密文件，不上传任何第三方服务
- 渲染进程开启 contextIsolation、关闭 nodeIntegration，系统调用全部走 preload 白名单
- 在线登录期间临时接管 `kiro://` 协议，登录结束或应用退出立即注销，不长期占用 Kiro IDE 的协议
- Enterprise SSO 回调服务器仅监听 `127.0.0.1` 随机端口，授权完成即关闭，state 与 PKCE 全程校验
- 外部链接只放行 http/https，统一交给系统浏览器打开

> 安装遇到问题？请查看 [安装说明与常见问题](./INSTALL.md)。

[1.0.21]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.21
[1.0.20]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.20
[1.0.19]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.19
[1.0.18]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.18
[1.0.17]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.17
[1.0.16]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.16
[1.0.15]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.15
[1.0.14]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.14
[1.0.13]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.13
[1.0.12]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.12
[1.0.11]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.11
[1.0.10]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.10
[1.0.9]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.9
[1.0.8]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.8
[1.0.7]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.7
[1.0.6]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.6
[1.0.5]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.5
[1.0.4]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.4
[1.0.3]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.3
[1.0.2]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.2
[1.0.1]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.1
[1.0.0]: https://github.com/lucks-cloud/kiro-manager-lite/releases/tag/v1.0.0
